import React from 'react';
import './Admin.css';

const Settings = () => {
    return (
        <div className="settings">
            <h2>Settings</h2>
            {/* Add settings content here */}
        </div>
    );
};

export default Settings;
